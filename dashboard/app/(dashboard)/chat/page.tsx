import ChatThreadLayout from "@/app/components/ChatThreadLayout";

export default function ChatPage() {
  return (
    <div className="h-[calc(100vh-80px)]">
      <ChatThreadLayout />
    </div>
  );
}
